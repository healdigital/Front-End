---
interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<div
  id={id}
  class:list={[
    "grid grid-cols-1 md:grid-cols-4 lg:grid-cols-12 gap-7",
    className,
  ]}
>
  <slot />
</div>
